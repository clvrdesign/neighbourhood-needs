import React from "react";

    function TextArea({handleProblemText, problemText}) {
      return (<textarea maxLength={200} rows={5} type="text" placeholder="Whats wrong in your Neighbourhood?" className="message" value={formData.message} onChange={handleProblemText} ref={problemText} />);
    }
  
  